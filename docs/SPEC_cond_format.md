# ⑧ 条件付き書式 — ★実Excelで測ってから書いた設計★（まだ1行も作っていない）

作成 2026-08-22 ／ 指示役の順番「8 条件付き書式 → 9 グラフ」
真値 = `tests/fixtures/cond-format-golden.json`（実Excel 16.0.20228 を COM で動かして測った）

---

## 0. ★測って分かった事（思い込みと違った所）★

| 何 | 実Excel | 思い込み |
|---|---|---|
| ★文字は 数より大きい★ | 「あいう」は ★「25より大きい」に当たる★ | 当たらないと思っていた |
| ★空セル★ | 「25より大きい」に当たらない／★「25より小さい」に当たる（0扱い）★ | どちらも当たらないと思っていた |
| ★空文字を返す式★ `=IF(1=1,"","x")` | ★文字扱い＝「より大きい」に当たる★ | 空セルと同じだと思っていた |
| ★エラー★ `#DIV/0!` | どちらにも当たらない | — |
| ★上位10項目の TopBottom★ | ★0＝下位／1＝上位★ | 0を上位だと思っていた |
| ★2本が重なった時★ | ★番号の小さいルールが勝つ★ | — |
| ★手で塗った色★ | 条件付き書式が勝ち、手の塗りは ★下に残る★ | 上書きで消えると思っていた |
| ★計算★ | ★1ミリも変わらない（見た目だけ）★ | — |

★測り方で2回 嘘をついた★（[[golden の _★測り方で2回 嘘をついた★]] に残した）
1. `=IF(A6>25,…)` で測った＝★式の答えであって「当たったか」ではない★
2. PowerShell で `色($a) -eq $c` と書いた＝★関数に3つ渡していて 比べていない★（全部「当たる」になった）
→ ★当たり判定は `Range.DisplayFormat.Interior.Color`（実際に描かれる色）で読む★

---

## 1. 作る物（第1弾）

★Excelの「ホーム→条件付き書式」の中で、中小企業が実際に使う物だけ★を先に作る。

| # | ルール | 何を聞くか | 優先 |
|---|---|---|---|
| 1 | セルの値が **より大きい / より小さい / 次の値に等しい / 指定の範囲内** | 数か文字を1つ（範囲内は2つ） | ★第1弾★ |
| 2 | **文字列を含む** | 文字を1つ（★大文字小文字を区別しない★） | ★第1弾★ |
| 3 | **重複する値 / 一意の値** | なし | ★第1弾★ |
| 4 | **上位/下位 N 項目** | N（既定10） | ★第1弾★ |
| 5 | **数式を使って書式設定** | 式1本（★範囲の左上から見て書く★） | ★第1弾★ |
| 6 | データバー / カラースケール / アイコンセット | — | 第2弾 |
| 7 | 日付（今日・今週…）／空白セル／エラー値 | — | 第2弾 |

### 当てる書式（第1弾）
★塗り・文字色・太字★ の3つだけ。Excelの既定の組み合わせ（★濃い赤の文字 `#9C0006` ／ 明るい赤の背景 `#FFC7CE`★＝実測値）を先頭に置く。

---

## 2. 決まり（実Excelに合わせる。勝手に決めない）

```
 ・当たり判定の型 … 数どうし＝数で比べる／文字が混ざれば ★文字は数より大きい★
                    空セルは ★0★／空文字を返す式は ★文字★／エラーは ★どのルールにも当たらない★
 ・2本以上が当たったら ★番号の小さい方が勝つ★（塗りと文字色のように 別項目なら 混ざる）
 ・手で塗った色は ★消さない★（条件を外したら 元の色に戻る）
 ・★計算には触らない★（SUM も 並べ替えも 絞り込みも 何も変わらない）
 ・式のルールは ★範囲の左上から見た式★。$ で止めた所は動かない
```

---

## 3. どこに置くか（Excelと同じ場所にする＝方針）

- ★右クリック★ … 「🎨 条件付き書式」（並べ替え・絞り込みの下）
- ★書式のバー★ … 「書式」の隣に置く
- 窓の中身は ★1画面で完結★（Excelの「新しい書式ルール」の 二段構えは真似ない＝迷わせない）
  1) どの範囲か（選んだ範囲を そのまま見せて、直せる）
  2) どうなったら（上の表の1〜5）
  3) どう見せるか（塗り・文字色・太字）
  4) ★今 何個 当たるかを その場で数えて見せる★（押す前に分かる）
- ★一覧★ … 当てたルールを並べ、★上下で順番を変えられる（番号の小さい方が勝つ）★・消せる

---

## 4. ファイル（.xlsx / .xlsb）の扱い

```
 読む … xl/worksheets/sheetN.xml の <conditionalFormatting sqref="…"><cfRule …>
        色は xl/styles.xml の <dxfs> を dxfId で指す
 書く … ★第1弾では 書き出さない（画面の中だけ）★
        理由＝dxfs を足すと styles.xml を作り替える事になり、
        ★「元のファイルを1バイトも触らない」という一番大事な約束に触れる★
 ★消さない事は 実測済★ … 客のファイルに元から在る条件付き書式は
        lib/xlsx-edit.js（値だけ書き換える道）を通しても ★そのまま残る★
        （元 9,704B → 書き出し 7,821B・中の conditionalFormatting は1文字も変わらず）
 ⇒ ★第2弾で「書き出しにも乗せる」を別に設計する★（その時は dxfs の足し方を測ってから）
```

---

## 5. 検査（作る前に決めておく）

```
 ① lib/cond-format.js（純関数）… 当たり判定だけ。真値 tests/fixtures/cond-format-golden.json と突き合わせる
    ・10/20/30/40/50・空・文字・空文字の式・エラー の8通り × ルール5種
    ・2本が重なった時の勝ち負け
    ・式のルールの ずれ方（=$A1>25 を A1:C3 に当てたら 3行目だけ 当たる）
 ② 本物の book.html に載せて ★実際に右クリック→窓→押す★（jsdom）
    ・当たった数が 画面に出る／消せる／順番を変えると 見え方が変わる
    ・★合計が1円も変わらない事を 押した後に数える★
 ③ --self-test … わざと壊して 全通り赤になる事
 ④ 実配信で 私が押す（画面の高さ 619/400 で 窓が収まるか＝★lib/menu-place.js を通す★）
```

---

## 6. やらない事（第1弾では）

- ★書き出しに乗せない★（上の4のとおり）
- データバー・カラースケール・アイコンセット（第2弾）
- 「セルの強調表示ルール」の 日付・空白・エラー（第2弾）
- ★上限も 既定オフも 足さない★（勝手に足すなという決まり）
